// Disposable production smoke fixture: the repository archive also contains
// a large ignored media blob, so the cloud scan must use its filtered tree.
export function evaluateUserInput(input) {
  return eval(input);
}
